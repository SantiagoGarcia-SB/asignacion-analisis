{
  "name": "asignacion-analisis-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Property-based tests para el motor de asignación (latency optimization)",
  "scripts": {
    "test:properties": "npx vitest --run tests/properties/",
    "test": "npx vitest --run"
  },
  "devDependencies": {
    "vitest": "3.2.7",
    "fast-check": "4.1.1"
  }
}
